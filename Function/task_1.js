/* 
Take four parameters. Multiply the four numbers and then return the result
 */

function fourMulti(num1, num2, num3, num4) {
    const res = num1 * num2 * num3 * num4;
    return res;
}
const res = fourMulti(2, 2, 3, 4);

console.log("The result is: ", res)